/**
 * Pacote uminho.grupo57.storage: implementação de persistência e gestão em memória.
 *
 * Contém classes responsáveis por persistir séries temporais em disco, gerir
 * caches e políticas de armazenamento (por exemplo `SeriesPersistence` e
 * `SeriesMemoryManager`).
 *
 * @since 1.0
 */
package uminho.grupo57.storage;
